import { Fanwood_Text, Inter, Limelight, Mulish, Oswald, Quattrocento } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });

export const mulish = Mulish({ subsets: ['latin'] });

export const oswald = Oswald({ subsets: ['latin'] });

export const quattrocento = Quattrocento({ weight: '400',subsets: ['latin'] });

export const fanWood = Fanwood_Text({ weight: ['400'],subsets: ['latin'] });

export const limelight = Limelight({ 
  weight: '400',
  subsets: ['latin']
});