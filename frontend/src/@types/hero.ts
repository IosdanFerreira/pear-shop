import { IProduct } from './product';

export interface IHero {
    title: string,
    subTitle: string,
    description: string,
    textColor: string,
    urlLink: string,
    backgroundImage: string,
}